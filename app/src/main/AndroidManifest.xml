<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rijke.itt_v1">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_weiss"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_rund"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Startseite"></activity>
        <activity android:name=".Registrieren"></activity>
        <activity android:name=".Historie"></activity>
        <activity android:name=".Start"></activity>
        <activity android:name=".Userarea"></activity>
        <activity android:name=".Userdaten"></activity>
        <activity android:name=".Social"></activity>
    </application>

</manifest>